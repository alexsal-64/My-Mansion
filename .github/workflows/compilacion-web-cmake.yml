name: Build Raylib Web Game with CMake

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Emscripten
        run: |
          git clone --depth 1 https://github.com/emscripten-core/emsdk.git
          cd emsdk
          ./emsdk install latest
          ./emsdk activate latest
          echo "EMSDK_ENV=$PWD/emsdk_env.sh" >> $GITHUB_ENV

      - name: Build raylib for Web (with CMake)
        run: |
          source $EMSDK_ENV
          git clone --depth 1 https://github.com/raysan5/raylib.git
          cd raylib
          mkdir build && cd build
          emcmake cmake -DBUILD_SHARED_LIBS=OFF -DCMAKE_INSTALL_PREFIX=$(pwd)/../dist ..
          emmake make -j2
          make install
          cd $GITHUB_WORKSPACE

      - name: Debug raylibConfig.cmake location
        run: |
          find $GITHUB_WORKSPACE/raylib/dist -name "raylibConfig.cmake" || true

      - name: Build game for Web with CMake
        run: |
          source $EMSDK_ENV
          emcmake cmake -B build-web-cmake \
            -DCMAKE_BUILD_TYPE=Release \
            -Draylib_DIR="$GITHUB_WORKSPACE/raylib/dist/lib/cmake/raylib"
          cmake --build build-web-cmake --config Release
        working-directory: ${{ github.workspace }}

      - name: Prepare and list web artifacts
        run: |
          mkdir -p build-web-cmake-dist
          cp build-web-cmake/bin/mansion.* build-web-cmake-dist/ || true
          cp -r assets build-web-cmake-dist/ || true
          ls -lh build-web-cmake-dist || true

      - name: Upload web artifacts
        uses: actions/upload-artifact@v4
        with:
          name: my_mansion-web-cmake
          path: build-web-cmake-dist/
